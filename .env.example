# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
# For local MongoDB: mongodb://localhost:27017/intellitask
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/ai-task-management?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_in_production_make_it_very_long_and_random
JWT_EXPIRES_IN=24h

# AI Configuration (Optional)
OPENAI_API_KEY=sk-your-api-key-here

# CORS Configuration (for development)
ALLOWED_ORIGINS=http://localhost:4200,http://localhost:3000,https://*.vercel.app

# Production Environment Variables (set in Vercel dashboard)
# VERCEL_ENV=production
# VERCEL_URL=your-deployment-url.vercel.app
